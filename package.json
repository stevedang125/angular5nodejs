{
  "name": "angular5_nodejs",
  "version": "1.0.0",
  "description": "Daily Goals with user register and login using the new Angular5",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && cd frontend && npm install && ng build && cd .. && node server.js"
  },
  "dependencies": {
    "express": "*",
    "body-parser": "^1.18.2",
    "cors": "*",
    "bcryptjs": "*",
    "mongoose": "*",
    "jsonwebtoken": "*",
    "passport": "*",
    "passport-jwt": "*"
  },
  "author": "steve",
  "license": "ISC"
}
